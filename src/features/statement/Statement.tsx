export function Statement() {
  return (
    <section
      id="statement"
      tabIndex={-1}
      aria-labelledby="statement-heading"
      className="home-statement layout-container"
    >
      <p className="eyebrow">01 / Intention</p>
      <h2 id="statement-heading">
        <span>Je conçois et développe</span>
        <span>des produits numériques.</span>
      </h2>
      <p className="statement-line body-large">
        De l’interface à l’infrastructure.
      </p>
    </section>
  );
}
